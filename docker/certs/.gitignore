# Local-only TLS certs generated by mkcert. Don't commit:
#   - They're per-developer (signed by each dev's local CA)
#   - They're not secrets exactly, but they're also not useful to anyone else
*.crt
*.key
*.pem
